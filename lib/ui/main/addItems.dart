import 'package:flutter/material.dart';

class Additems extends StatelessWidget {
  const Additems({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Additems')),
      body: Center(child: Text('Additems')),
    );
  }
}
