import 'package:flutter/material.dart';

class CWWBoardPage extends StatelessWidget {
  const CWWBoardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('CWW Board')),
      body: Center(child: Text('Nội dung trang CWW Board')),
    );
  }
}
