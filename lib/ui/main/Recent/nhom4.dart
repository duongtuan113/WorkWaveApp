import 'package:flutter/material.dart';

class Nhom4BoardPage extends StatelessWidget {
  const Nhom4BoardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('NHOM4 Board')),
      body: Center(child: Text('Welcome to NHOM4 Board')),
    );
  }
}
