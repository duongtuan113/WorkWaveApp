import 'package:flutter/material.dart';

class Databasedesignpage extends StatelessWidget {
  const Databasedesignpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Databasedesignpage')),
      body: Center(child: Text('Databasedesignpage')),
    );
  }
}
